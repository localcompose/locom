name: Release smoke test

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder success
        run: echo "This is a placeholder workflow to register 'Release smoke test' in Actions. Always succeeds."
